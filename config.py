# Wi-Fi credentials
WIFI_SSID = "YOUR_WIFI"
WIFI_PASSWORD = "YOUR_PASSWORD"

# should match config on apollo-server
APOLLO_HOST = "apollo"
APOLLO_PORT = 5557
IMAGE_PORT = 5556

# UI OVERLAY
# If on current player's state icon will be drawn
UI_SHOW_PLAYER_ICON = True
# If on current artist will be shown on top
UI_SHOW_ARTIST_TOP = True
# If on current track name will be shown at the bottom
UI_SHOW_TRACK_BOTTOM = True
# Timeout for UI overlay. Only applicable when player is playing.
# On paused state overlay is always shown
UI_OVERLAY_HIDE_DELAY_S = 10
# When paused or stopped, the board will stop rendering updates on screen in N minutes.
UI_SLEEP_TIMEOUT_MIN = 1
